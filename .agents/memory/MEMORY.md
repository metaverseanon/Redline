# Memory Index

- [RedLine backend trust model](redline-backend-trust-model.md) — api-server tRPC has NO auth (publicProcedure + client userId); never trust client-asserted Pro/entitlement claims, re-verify server-side via RevenueCat REST.
- [Supabase REST once-only mutex](supabase-rest-once-only-mutex.md) — no DB transactions over REST; use conditional PATCH (`status=eq.X` + `return=representation`) as a mutex so reward/grant side-effects run exactly once.
- [Challenges round lifecycle](challenges-lifecycle.md) — transitions are side effects of getActiveChallenge (traffic-driven + 5-min in-process ticker; autoscale = no cron); end = perfect score OR 2-wk timer; maxPointsForTasks must mirror scoreUser (uncapped progressive ⇒ Infinity, disable early-finalize).
- [EAS build in Replit (git lock)](eas-build-in-replit-git-lock.md) — `eas build` fails on `.git/index.lock` in the sandbox; prefix `EAS_NO_VCS=1`, use `--no-wait --auto-submit`, delete stale lock via code-exec fs.unlinkSync.
- [Meta fbsdk-next ATT gating](meta-fbsdk-att-gating.md) — keep `isAutoInitEnabled:false`; Meta SDK inits only via `initializeMeta()` after ATT resolves, else advertiser-ID collection starts before consent.
- [Email image hosting](email-image-hosting.md) — email images need public HTTPS URLs (attached_assets/ + data-URIs fail); serve from deployed api-server, version the path, and redeploy prod before sending.
- [Apple relay email bounce](apple-relay-email-bounce.md) — @privaterelay.appleid.com welcome emails bounce (gmail delivers); NOT a code bug — register redlineapp.io in Apple Dev "Sign in with Apple for Email Communication".
- [Apple Sign-In real email display](apple-signin-real-email-display.md) — real Apple email is captured client-only (AsyncStorage appleEmail) since no Supabase DDL; synthetic email stays the identity key; resets on reinstall unless re-link.
- [Apple Sign In identity](apple-sign-in-identity.md) — key Apple accounts on synthetic email from stable credential.user (email only sent on first auth); backend returns existing account on apple re-auth so client adopts canonical id.
- [Ad SDK conversion events](ad-sdk-conversion-events.md) — RedLine fires Subscribe/Purchase to ad SDKs client-side from purchaseMutation (lib/revenuecat.tsx) + identify in _layout.tsx, NOT via RevenueCat S2S; each SDK has a lib/<sdk>.ts wrapper.
- [Superwall over RevenueCat](superwall-revenuecat-layering.md) — Superwall owns paywall UI, RC stays purchase backend; settle results from SW callbacks only (never timed), null=fallback, no placeholder key in eas.json, circular imports via namespace.
- [User insert duplicate-key storms](redline-user-insert-duplicates.md) — register+ensureUser must check ALL unique constraints (id/email/display_name) not just id; client retries turn one doomed insert into a 409 flood.
- [RedLine register return shape](redline-register-return-shape.md) — user.register returns {success:false} (no throw) for existing email/Google, {upgraded:true} for pwd-add, {existing:true} for apple re-auth; gate "new account" logic accordingly.
- [Speed-camera directional warnings](speed-camera-directional-warnings.md) — warn only for cameras in a ~75° forward cone, heading gated by min speed (noisy when slow); trip-count detection stays radial & direction-independent.
- [h3-js Hermes boot crash](h3-js-hermes-boot-crash.md) — never statically import emscripten/asm.js modules (h3-js) on the RN boot path; lazy-require + cache + try/catch, else release Hermes crashes on launch.
- [Reanimated web entering blanks](reanimated-web-entering.md) — RN-web: Reanimated `entering` (FadeIn/FadeInDown), esp. nested, renders content blank (opacity:0, no error); gate entering to undefined + swap to plain View/Image on web. White=layout artifact, black+red=real crash.
- [Territory Claiming Game](territory-claiming-game.md) — H3 cell territory feature; server-authoritative Pro re-verify, persisted-flag contract (mark trip recorded only after server confirms), manual Supabase DDL for 2 new tables.
